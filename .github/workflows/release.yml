name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    runs-on: macos-14
    permissions:
      contents: write
      
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Get version from tag
      id: version
      run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
    
    - name: Update version in Info.plist
      run: |
        VERSION=${{ steps.version.outputs.VERSION }}
        sed -i '' "s/<string>1\.0\.0<\/string>/<string>${VERSION}<\/string>/" Resources/Info.plist
        cat Resources/Info.plist | grep -A1 CFBundleShortVersionString
    
    - name: Resolve dependencies
      run: |
        swift package resolve
        echo "✅ Dependencies resolved"
    
    - name: Build app
      run: |
        set -e
        swift build -c release
        echo "✅ Swift build complete"
        
        # Create app bundle structure
        APP_NAME="droplet"
        APP_BUNDLE="${APP_NAME}.app"
        CONTENTS_DIR="${APP_BUNDLE}/Contents"
        MACOS_DIR="${CONTENTS_DIR}/MacOS"
        RESOURCES_DIR="${CONTENTS_DIR}/Resources"
        FRAMEWORKS_DIR="${CONTENTS_DIR}/Frameworks"
        
        echo "Creating app bundle..."
        rm -rf "${APP_BUNDLE}"
        mkdir -p "${MACOS_DIR}" "${RESOURCES_DIR}" "${FRAMEWORKS_DIR}"
        
        # Copy executable
        cp ".build/release/${APP_NAME}" "${MACOS_DIR}/"
        chmod +x "${MACOS_DIR}/${APP_NAME}"
        
        # Copy Info.plist
        cp "Resources/Info.plist" "${CONTENTS_DIR}/"
        
        # Copy resources
        if [ -d "Resources/Sounds" ]; then
          echo "Copying sounds..."
          cp -r "Resources/Sounds" "${RESOURCES_DIR}/"
        fi
        
        if [ -f "Resources/AppIcon.icns" ]; then
          echo "Copying icon..."
          cp "Resources/AppIcon.icns" "${RESOURCES_DIR}/"
        fi
        
        # Sign the app (ad-hoc) to avoid "damaged app" errors
        echo "🔐 Signing app..."
        codesign --force --deep --sign - "${APP_BUNDLE}"
        
        echo "✅ App bundle created: ${APP_BUNDLE}"
        
    - name: Create DMG
      run: |
        set -e
        
        # Install create-dmg utility
        brew install create-dmg
        
        # Create DMG using create-dmg for a prettier layout
        echo "Creating DMG..."
        create-dmg \
          --volname "droplet" \
          --volicon "Resources/AppIcon.icns" \
          --window-pos 200 120 \
          --window-size 600 300 \
          --icon-size 100 \
          --icon "droplet.app" 175 120 \
          --hide-extension "droplet.app" \
          --app-drop-link 425 120 \
          "droplet-installer.dmg" \
          "droplet.app"
        
        # Verify DMG was created
        ls -lh droplet-installer.dmg
        echo "✅ DMG created successfully"
    
    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        name: droplet v${{ steps.version.outputs.VERSION }}
        body: |
          ## droplet v${{ steps.version.outputs.VERSION }}
          
          ### Installation
          1. Download `droplet-installer.dmg`
          2. Open the DMG and drag droplet to Applications
          3. Launch from Applications or Spotlight
          
          > **Note**: On first launch, right-click the app and select "Open" to bypass Gatekeeper.
          
          ### What's New
          - ✨ **Task Management**: Create tasks, track time per task, and view your focus history.
          - 🎨 **Mini-Floater Mode**: Toggle a tiny, pill-shaped window (100x36) to keep an eye on time without distraction.
          - 🎭 **New Themes**: Added **Leaf** (Deep Pine) and **Navy+** (Dark Navy/Beige).
          - ⏱️ **Menu Bar Timer**: See the countdown directly in your macOS menu bar.
          - ⚙️ **Enhanced Settings**: Unified, easier-to-use settings experience with improved customization.
          - 🐛 **Bug Fixes**: Smoother animations, fixed progress bar scaling, and improved stability.
        files: droplet-installer.dmg
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  